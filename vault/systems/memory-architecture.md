Episodic + semantic + procedural memory for AI agents. Episodic: vector DB with temporal indexing, decayed retrieval, recall triggered by similarity + recency + importance. Semantic: knowledge graph + parameterized facts via continued fine-tuning. Procedural: tool-use traces stored as reusable skills. Consolidation pipeline: nightly 'sleep' that replays important episodes and integrates them into semantic memory via fine-tune.
