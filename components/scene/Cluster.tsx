/**
 * Cluster.tsx
 *
 * One bridge cluster — a group of endpoint glows + satellites.
 * Reused 7x in the scene, one per bridge.
 * - Glow at cluster center
 * - Satellites orbit around it (Beat 4 only)
 *
 * Props: cluster index, visibility/fade
 *
 * See SECTION 2 BEAT 3–4
 */

export default function Cluster() {
  // TODO: Implement Cluster
  // - Render cluster glow (small sphere, emissive)
  // - Placeholder for satellites (rendered by Satellites component)
  // - Position based on cluster index

  return null;
}
