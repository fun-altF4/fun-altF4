/**
 * DualCore.tsx
 *
 * Two interweaving hemispheres with emissive shader gradients:
 * - Hemisphere A (technical): #4F7CFF → #A66CFF
 * - Hemisphere B (philosophical): #FFB347 → #FF6E5C
 * - Rotate around each other at slightly different speeds, never touching
 * - Custom GLSL shader with simplex noise for surface displacement
 * - Additive blending + bloom post-processing
 *
 * Triggered: Beat 1 (scroll 0–18%)
 * Used in: Beats 1–5 (shrinks as camera pulls back in Beat 3)
 *
 * See SECTION 2 BEAT 1 and SECTION 5 (shader files)
 */

export default function DualCore() {
  // TODO: Implement DualCore
  // - Load hemisphere.vert + hemisphere.frag shaders
  // - Create two sphere geometries
  // - Apply emissive shader materials with color gradients
  // - Implement rotation animation in useFrame
  // - Add to scene

  return null;
}
