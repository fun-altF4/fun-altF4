/**
 * MirrorBadge.tsx
 *
 * Mirror message styling:
 * - 2px accent bar (left side, full message height, #FF4D6D)
 * - Small label above message: "MIRROR" (10px JetBrains Mono, #FF4D6D, 60% opacity)
 *
 * Rendered when isMirror = true on a message.
 * After 4–6 turns, agent may surface mirror observations.
 *
 * See SECTION 2 TIER 3 (Mirror Outputs) and SECTION 6.1 (MIRROR_PROTOCOL)
 */

export default function MirrorBadge() {
  // TODO: Implement mirror badge
  // - Left accent bar styling
  // - Label rendering
  // - Conditional display

  return null;
}
