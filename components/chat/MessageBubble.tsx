/**
 * MessageBubble.tsx
 *
 * Individual message component:
 * - User messages: right-aligned, subtle blue tint (#4F7CFF/15%), padding 12px 16px, rounded 12px
 * - Agent messages: left-aligned, white text on transparent background, no bubble
 * - Supports mirror badge (separate component)
 * - Markdown rendering (if needed)
 *
 * See SECTION 2 TIER 3 (Layout section)
 */

export default function MessageBubble() {
  // TODO: Implement message bubble
  // - Props: message (Message type), isMirror
  // - Conditional styling (user vs agent)
  // - Mirror badge injection
  // - Auto-scroll parent on render

  return null;
}
