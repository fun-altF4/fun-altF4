/**
 * PersonaIndicator.tsx
 *
 * Small chip (10px font, gray-500) showing detected visitor type.
 * Positioned: top-right of chat container
 * Extremely subtle — visitor should barely notice unless they look.
 *
 * Possible values: "engineer", "founder", "philosopher", "recruiter", "curious", "ambiguous"
 * Updated after visitor classification via /api/classify
 *
 * See SECTION 2 TIER 3 (Persona Indicator)
 */

export default function PersonaIndicator() {
  // TODO: Implement persona indicator
  // - Use useStore(state => state.visitorType)
  // - Position: top-right of chat
  // - Subtle styling (small, gray, opacity)

  return null;
}
